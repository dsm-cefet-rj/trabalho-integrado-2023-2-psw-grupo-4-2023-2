import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#031881',
        },
        secondary: {
            main: '#E6B33D'
        },
        background:{
            default: '#ECF0F2'
        }
    },
    typography:{
        
    }
});


export default theme
